@prefix p <http://uri.org/>
@prefix sbol <http://sbols.org/v2#>
@defaultPrefix sbol

A(x, y) =>
  x = 42
  <http://example.eg/predicate> = y

B() => A(x, "VALUE")
  x = p.y
  z = "STRING"

C(x) => B()
  x = 43
  z = "STRING"

D(x) =>
  h = g : C(x)
  y = "That one"

t : C(<http://uri.org/x>)
  v = True
  s : D(<http://uri.org/x>)